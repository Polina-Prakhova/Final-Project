sudo: false

language: python
python:
    - "3.8"
install:
    - pip install pylint
    - pip install -r requirements.txt
script:
    - pylint --output-format=text department-app
    - coverage run -m unittest department-app/tests/
after_success:
    - coveralls
